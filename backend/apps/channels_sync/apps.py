from django.apps import AppConfig

class ChannelsSyncConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.channels_sync'
    verbose_name = 'Channel Synchronization'
